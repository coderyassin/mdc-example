server:
  port: 7070
  address: 0.0.0.0
spring:
  application:
    name: sleuth-example
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    env:
      show-values: always

logging:
  level:
    org:
      springframework:
        web:
          servlet:
            DispatcherServlet=DEBUG: